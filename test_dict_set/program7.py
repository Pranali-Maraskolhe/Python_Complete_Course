# Can you change the values inside a list which is contained in set S?

s = {8, 7, 12, "Harry", [1,2]}

# we can't include set inside set
# Set in python required all their elements to be immutable and hashable